<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Theme for your app -->
    <style name="Theme.ChatApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your app's theme attributes -->
    </style>

    <!-- Theme for your splash screen -->
    <style name="Theme.SplashScreen.MySplash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.ChatApp</item>
    </style>

</resources>
